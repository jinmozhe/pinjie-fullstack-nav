# 母版 90644f5 累计更新计划

## 当前状态和结果

- 状态：已结束
- 结果：已完成；从共同祖先累计吸收适用通用修复并完成轻量门禁，候选镜像工具等母版身份绑定内容排除

## 关联需求

- `NAV-GEN-001`、`NAV-QUAL-001`：保留独立导航业务及既有能力。
- `BASE-SEC-002`、`BASE-SEC-006`、`BASE-QUAL-002`、`BASE-QUAL-004`、`BASE-OPS-007` 至 `BASE-OPS-009`、`BASE-DOC-004` 至 `BASE-DOC-006`。

## 背景

用户要求将 Nav 继承的母版能力更新至指定版本，授权本地修改与必要轻量验证，明确禁止提交、推送、PR、发布和生产操作。

## 目标

1. 比较原派生快照到目标提交的累计变化，逐项引入适用修复并保留 Nav 业务语义。
2. 保留原发布基线与派生快照，在验证完成后单独登记选择性迁移范围与排除项。
3. 同步适用源码、验证工具、配置及文档，提供未来应用更新范围。

## 非目标

- 不执行 production build、Vitest、pytest、Playwright、浏览器验证、测试数据库迁移、云构建或部署。
- 不提交、暂存、推送、创建 PR、发布镜像或添加永久母版远程。
- 不复制母版身份、历史计划、真实环境文件或运行数据，不替换 Nav 功能、UI、独立会话和配置。

## 现状分析

- 母版仓库：[pinjie-fullstack-base](https://github.com/jinmozhe/pinjie-fullstack-base)。
- 原发布基线：`v1.0.0`，`7f5f4ad28b73ffabe3c0fa9cc99b39875482ba18`。
- 实际派生快照及共同祖先：`00b409ae866b260854b75bef74c16cd4b9692c79`。
- 目标：`90644f57177df7e404900516dedcb4a107e3bbdc`。
- 开始时分支 `codex/nav-derived-baseline`，工作区与暂存区干净，仅有 Nav 的 `origin`，无自动同步脚本或既往同步计划。
- 累计范围包含 5 个提交、61 个文件；发布 Tag 到派生快照之间的治理提交已经继承，不重复迁移。
- Nav 已有 `AdminRepository.get(refresh=True)` 权限刷新、导航图标引用保护、独立 reader CSRF/失效事件、授权页和 Nginx 代理例外，需要保留。
- 原有 CNB/TCR 配置仍含母版模板，不能视为 Nav 发布环境已就绪。

## 方案设计

### 同步机制

沿用 PRD 第 16 节的专项计划与定向吸收机制。临时按 URL 获取目标 Git 对象，不增加远程。对共同祖先与目标的差异逐文件应用补丁；交叉修改按三方源码人工适配，不创建 merge/cherry-pick 提交或整目录覆盖。

### 累计变化处置

| 上游提交 | 内容 | 处理 |
| --- | --- | --- |
| `da1c33db97a10e68e9a428d1cec1cbc0288e3690` | 权限、会话、头像；规则与构建交付工具 | 引入通用部分，适配 Nav；候选镜像工具与母版历史记录排除 |
| `143e0e9282459f00a7836b97e87f1c64e8c01edc` | 严格扫描、矩阵变量与发布子进程限制 | 引入适用扫描和工作流修复；专属候选工具子进程修改随工具排除 |
| `a048b1cb88a0b0fe5695095187885db9fdce3415` | Admin 重复安全响应头 | 引入并保留导航代理 |
| `23f17c6998bd739b56f0bb56b217fdeaf5ceadab` | 简化单维护者人工部署步骤 | 适配通用手册，保留 Nav 配置与历史来源 |
| `90644f57177df7e404900516dedcb4a107e3bbdc` | CNB 累计范围与 TCR 标签用途 | 引入适用说明 |

### 排除项

- 母版新增的三份 `plans/2026-09-06_*计划.md` 及其 `plans/INDEX.md` 登记、母版 Changelog 历史不复制；Nav 单独记录实际实施结果。
- `.github/workflows/validate-candidate-images.yml`、`scripts/release/candidate-images.mjs`、`scripts/release/composition.mjs`、`scripts/release/github-evidence.mjs`、`scripts/release/release-tools.mjs`、`scripts/ci/fixtures/candidate-processes.mjs`、`docs/operations/candidate-image-validation.md`：可选组合镜像工具绑定母版 GitHub/CNB/TCR 身份，Nav 尚无准确的独立发布配置，不能直接启用或猜测地址。
- `scripts/ci/test-release-pipeline.mjs` 及根 `package.json` 中该检查入口：候选镜像工具专属夹具排除；其中通用扫描过滤断言迁入既有扫描门禁。
- 上游文档索引、ADR、运维手册中关于上述可选工具可用性的陈述不引入；其余适用变化就地合并。
- 不把目标 SHA 当作新的完整母版基线；后续同步仍以原派生快照加本记录的逐项差异为依据。

## 全栈影响矩阵

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 涉及 | 锁定管理员时刷新身份、头像资产引用 | 现有事务与资产机制 | Ruff、格式、Mypy、导入边界、编译、应用导入与契约 |
| Admin | 涉及 | 会话错误与刷新故障、代理安全头 | Backend 错误码 | typecheck、lint |
| Web | 涉及 | 会话恢复错误及重试，保留 reader 隔离 | 既有 HTTP 管道 | typecheck、lint |
| API Client | 涉及 | 核验契约与生成漂移，预计无结构变化 | Backend | 导出、生成、零差异 |
| Database | 不涉及 | 无上游模型或迁移变化，Nav 迁移保留 | 无 | 差异核对；不连接数据库 |
| Deployment | 涉及 | Docker 缓存层、人工验证工具、CNB 扫描和交接证据 | 既有配置 | 静态门禁与隔离脚本夹具，不执行真实构建或发布 |
| Documentation | 涉及 | 规则、计划、架构与手册定向同步 | 变更审阅 | Markdown、文本、文档、工作区、模块边界 |

## 实施顺序

1. 核对共同祖先和完整累计差异，登记计划与活动索引。
2. 逐文件迁移通用源码及工具，再适配 Nav 交叉修改。
3. 同步适用规则、架构和运维手册，列明排除项。
4. 执行三端轻量门禁、契约生成与治理检查，修复本次引入的问题。
5. 复读差异、清理临时产物，最后更新选择性同步记录及计划结果。

## 影响文件

- Backend Repository、Service 及测试源码；Admin/Web HTTP、会话入口及测试源码。
- 两端 Dockerfile、Admin Nginx、`.cnb.yml`、适用工作流、CI/E2E 脚本与 `playwright.config.ts`。
- 根及受影响应用规则、`plans/README.md`、本计划、两份索引、Changelog 和适用 `docs/`。

## 风险与回滚

- 权限刷新和头像锁涉及并发一致性，真实 PostgreSQL 回归未获授权；保留并引入测试源码供后续专项执行。
- Web reader 不允许触发普通用户 Refresh 或普通用户失效事件，需专门复核和补充回归源码。
- 构建和交接脚本只做轻量验证，真实云平台行为未验证，旧 v1 完整验证证据不能满足 v2 交接门禁。
- 初始工作区干净，以 HEAD 和本任务差异定位恢复内容；仅反向恢复本次差异，禁止覆盖用户后续修改。不操作数据库或 Git 历史。

## 验证清单

- [x] 核对原基线、共同祖先、5 个累计提交和 61 个文件。
- [x] 三端轻量门禁与契约漂移。
- [x] 文本、Markdown、文档、工作区、模块边界、依赖、生产配置及脚本夹具。
- [x] Nav 交叉修改复核、选择性范围登记与临时资源清理。
- 明确未执行：所有 build、Vitest、pytest、Playwright、数据库迁移、云构建与生产操作。

## 待确认问题

无影响本次本地实施的待确认问题。独立发布身份及候选镜像工具接入列为排除项，不猜测生产配置。

## 用户确认记录

- 2026-09-06：用户明确要求先分析再直接完成指定 SHA 的范围内本地更新，并授权必要轻量验证；本记录引用该授权，不追加计划审批。
- 初始实施授权不包含提交、推送、PR、镜像发布、生产操作及本计划列出的重型验证。
- 2026-09-06：用户追加授权为本次已验证的母版更新创建本地检查点提交，不包含推送、PR、发布、部署或重型验证。

## 实施结果

已完成：按原派生快照 `00b409ae866b260854b75bef74c16cd4b9692c79` 到目标 `90644f57177df7e404900516dedcb4a107e3bbdc` 的 5 个提交累计差异，迁移会话错误传播、管理员身份锁定刷新、头像资产一致性、Admin Nginx 安全头、生产 E2E 服务归属、扫描证据结构化校验、计时与脱敏报告及 Docker 依赖层修复；保留 Nav reader 会话、导航图标引用保护、业务页面和配置。重新导出 OpenAPI 并生成 API Client，契约无漂移。

验证通过：Admin/Web typecheck 与 lint；Backend Ruff、格式、Mypy、lint-imports、compileall、应用导入与 OpenAPI；根文本、Markdown、文档、工作区、依赖、模块边界、生产配置、治理 guards、CNB 扫描/交接夹具；`git diff --check`。

- Nav 适配：`get(refresh=True)` 保留，`for_update=True` 同样刷新 ORM 状态；reader 的 CSRF 与失效事件继续独立，不调用普通用户 Refresh；授权页初始化例外、导航图标上传与删除引用保护、Nginx 导航配置代理均保留。补充 reader 隔离及临时错误回归源码，未运行 Vitest。
- 依赖、模型、迁移、配置模板与生成契约在母版累计范围内没有变化；保留 Nav 原有锁文件、环境模板、迁移与 76 个 API 路径，导出并生成后 `git diff --exit-code -- openapi.json packages/api-client/src` 通过。
- 文档门禁首次发现根活动索引状态包含额外描述，已修正并重跑通过。原先只写在候选工具夹具中的通用扫描过滤、人工工作流触发及阶段顺序断言迁入现有 `test-cnb-scan-images.mjs`。
- 累计 61 个上游路径逐一核对目标 Git blob；未直接匹配的文件均为 Nav 适配、当前项目记录或本计划明确排除项，不据此推进完整同步基线。
- 未来交付本次运行修复需要更新 Backend、Admin、Web 三端镜像；可选请求日志消费者若运行，也应随 Backend 镜像更新。本次无新增数据库迁移，Nav 既有未执行迁移及权限同步仍按导航启用手册单独处理。
- 清理本次编译产生的 Backend `__pycache__`；未启动项目服务、浏览器、数据库或容器。实现阶段未执行 Git 写入；后续按追加授权创建本地检查点。未推送、创建 PR、发布或部署，Git 远程仍仅有 Nav `origin`。

## 剩余问题

- 候选镜像组合工具与独立发布配置未接入；本次为选择性迁移。
- 真实并发、浏览器、生产构建和部署验证不在本次授权范围。
- 母版新增历史计划、Changelog 及绑定母版 GitHub/CNB/TCR 身份的候选镜像工作流与脚本未迁入。
