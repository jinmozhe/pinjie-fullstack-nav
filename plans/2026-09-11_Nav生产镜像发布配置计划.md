# Nav 生产镜像发布配置计划

## 1. 当前状态

- 状态：已结束
- 结果：已完成；CNB、TCR 镜像映射、发布证据、生产 Compose 模板和 Backend 项目标识已切换为 Nav 配置，实际 CNB 构建、TCR 推送和 1Panel 部署未执行
- 目标：将 Nav 的 CNB/TCR 生产镜像发布边界从母版标识切换为已创建的 Nav CNB 仓库和 TCR 镜像仓库

## 2. 关联需求

- NAV-GEN-001
- NAV-GEN-003
- BASE-DEP-001

## 3. 已确认资源

- CNB 仓库：`pjwl/pinjie-fullstack-nav`
- 共享 CNB secrets：`pjwl/pinjie-fullstack-base-secrets`
- TCR namespace：`pinjie-fullstack-base`
- Backend 镜像：`pinjie-nav-backend`
- Web 镜像：`pinjie-nav-web`
- Admin 镜像：`pinjie-nav-admin`

## 4. 实施范围

- 更新 CNB 仓库、TCR 镜像名和 GitHub 源仓库的 Fail Closed 校验。
- 保留共享 TCR secrets 导入地址及现有 TCR namespace。
- 更新镜像发布证据、扫描脚本、测试夹具和生产环境模板。
- 不修改 TCR 凭据内容，不执行 CNB 构建、镜像发布或生产部署。

## 5. 风险与回滚

- 旧母版镜像和 Nav 镜像名称不能混用；校验失败时应停止发布。
- 回滚使用原提交和已存在的完整镜像 digest，不使用可变标签。
- 若 CNB 或 TCR 资源名称与本计划不一致，应先调整配置或资源，不放宽校验。

## 6. 验证

- [x] CNB 发布、扫描和证据守卫测试通过。
- [x] 生产 Compose 配置检查通过。
- [x] Markdown lint 通过。
- [x] Backend Ruff、format、Mypy、导入边界、编译和应用导入通过。
- [x] `git diff --check` 通过。
- [ ] GitHub Push CI 通过。
- [ ] 实际 CNB 构建、TCR 推送和 1Panel 部署未执行。

## 7. 用户确认记录

- 2026-09-11，用户确认复用 `pjwl/pinjie-fullstack-base-secrets`，并已创建 `pjwl/pinjie-fullstack-nav` 及三个 TCR 镜像仓库。
