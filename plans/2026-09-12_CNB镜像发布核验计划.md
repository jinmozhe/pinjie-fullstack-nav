# CNB 镜像发布核验计划

## 状态、目标与授权

- 状态：已结束
- 结果：已完成；三端正式镜像及 digest 已核验，操作文档修订已通过本地验证；生产部署不在本计划范围内。
- 关联需求：BASE-DEP-001；核验既有发布链路，不增加产品能力。
- 2026-09-12，用户授权执行 `$git-sync`、检查 CNB、确保 TCR 存在镜像，并直接修复及重跑失败链路。生产部署不在范围内。
- 2026-09-12，用户要求提供详细的权限和登录操作文档，由用户按文档自行设置。补充现有发布手册第 2.1 至 2.6 节及 TCR 手册第 10.4 节，不代替用户操作凭据或权限。
- 初始目标版本：`9282a284c14e532f5a2b939ce0945d45f7f7f03e`；后续按实际失败修复推进。当前已核验 Backend 发布版本为 `68d391ae9523edbf22fb3f08fa994f5b14c442fc`，Web/Admin 为 `d80cf56b31aa1fd10e7d444956713044250dd094`。这些是镜像发布追溯 SHA，不随仅文档提交变化。
- 2026-09-13，用户要求解释本机 Docker 登录与 TCR 缓存故障，并将已验证处理方式整合到现有规范文档。定向更新现有发布、构建、账号手册和文档索引；不变更凭据、发布脚本或生产环境。

## 现状与方案

初始诊断时，目标版本的 GitHub 四项 Push CI、Full Validation 和 strict 源码交接已成功。本机 CNB API 未登录，Docker 查询 TCR 返回 unauthorized，浏览器工具不支持当时的认证方式。新增人工只读诊断工作流，在原 `cnb-source-handoff` Environment 内使用既有凭据查询固定 CNB 仓库，不复制凭据到本地、不修改权限、不触发构建。

按 CNB 官方 OpenAPI 查询目标 SHA 的构建列表和各阶段状态。输出仅包含构建标识、流水线与阶段状态，失败显式传播；诊断运行成功仅代表查询完成，不代表镜像发布成功。进一步依据实际失败阶段修复，镜像存在性须由 TCR digest 和发布证据确认。

## 全栈影响

| 范围 | 是否涉及 | 工作内容 | 前置依赖 | 验证 |
| --- | --- | --- | --- | --- |
| Backend | 不涉及 | 保留应用源码 | 无 | 复用目标版本 CI |
| Admin | 不涉及 | 保留应用源码 | 无 | 复用目标版本 CI |
| Web | 不涉及 | 保留应用源码 | 无 | 复用目标版本 CI |
| API Client | 不涉及 | 契约不变 | 无 | 无需重新生成 |
| Database | 不涉及 | 不执行迁移 | 无 | 未执行 |
| Deployment | 涉及 | CNB 状态查询与镜像核验 | 原环境凭据及已有发布 | 云端查询、发布证据和 digest |
| Documentation | 涉及 | 计划、索引和诊断入口说明 | 实际结果 | Markdown、文档和治理门禁 |

## 规则落实与实施

- 根规则及开发流程第 17、18.1 节：精确暂存，通过 PR 交付，保留必需检查；只执行适用轻量门禁。
- ADR 0008：固定目标 SHA，保留 strict 验证、镜像扫描和不可变标签约束；查询失败不降低发布门禁。
- SECURITY：凭据仅在原受保护环境使用；固定请求目标、只读 GET、拒绝重定向，不输出原始响应或凭据。
- 影响文件：`.github/workflows/inspect-cnb-release.yml`、现有发布手册、TCR 账号手册、`docs/README.md`、`CHANGELOG.md`、本计划与两个索引。
- 顺序：登记计划，实现并检查只读入口，完成 Git 交付，查询 CNB，再根据证据处理发布链路。

## 风险与回滚

既有 Token 可能缺少 `repo-cnb-history:r` 或 `repo-cnb-trigger:r`。缺权时报告准确权限，不自行修改凭据或扩大授权。工作流无自动触发和远端写入；回滚通过普通 PR 撤销入口，不影响已存在镜像。

## 验证与结果

- 已核对原目标 PR #19 合并及 13 项必需检查成功，本地和远端 main 一致。
- 原目标 Full Validation Run：`34657410029`，strict Handoff Run：`34658334142`，均成功。
- 新增入口通过 Markdown、文档治理、文本编码、工作区和模块边界检查；YAML 与内嵌 JavaScript 语法辅助核验通过。PR #20 的 13 项必需检查全部通过，已 rebase 合并，补丁对应及文件树核对一致。
- 云端诊断 Run `34666018877` 已执行。固定仓库与默认分支校验通过；使用原 Environment Token 读取 CNB 构建历史时返回 HTTP 403，查询失败。该结果不能用于判断 CNB 构建成功或失败。
- 本机 TCR Backend SHA 标签查询已执行；宿主 Docker 返回 unauthorized，无法核验镜像是否存在。沙箱凭据读取边界已通过准确宿主命令排除。
- 本次未执行应用 build、pytest、Vitest、Playwright、数据库迁移或生产部署。
- 操作文档补充：Token 权限表与替代流程、GitHub Environment Secret 更新、只读诊断运行、Windows TCR 登录、三仓顶层 digest 查询和错误分流；保留共享凭据的原使用方，避免已有可用密码被无条件重置。
- 官方依据：重新读取 CNB Token 与 Git 认证、GitHub Secrets、Docker login、腾讯云个人版密码与 CAM 授权文档。CNB OpenAPI 定义的再次查询遇到 DNS 失败，权限名称沿用本任务此前成功读取的定义；未实测的控制台按钮差异已在手册标明。
- 操作文档通过 Markdown、文档治理、UTF-8 与末尾换行检查，三个 PowerShell 代码块通过语法解析辅助核验；未执行其中的登录或镜像查询命令。已修正新增文本中 Bearer 前缀的行内代码空格告警，并同步文档索引入口说明。

## 后续核验与操作规范补充

- 2026-09-13，GitHub Full Validation Run `34731746072` 与 strict Handoff Run `34732768856` 成功。诊断 Run `34733047712` 确认 CNB `cnb-69o-1k2c91csm` 的 Backend 构建、扫描门禁、正式标签与证据保存成功，Web/Admin 因无相关变更跳过。
- 诊断 Run `34708897821` 确认 CNB `cnb-h23-1k2b8pt1c` 的 Web/Admin 上述步骤均成功；该次 Backend 在漏洞门禁失败，后续通过系统包升级修复。Git 差异确认两次源码之间只修改 Backend Dockerfile，未变化端可保留原发布版本。
- 本机已用 `docker buildx imagetools inspect` 核验三个正式镜像，OCI 来源均为本 Nav GitHub 仓库，revision 与各自发布 SHA 一致，平台为 `linux/amd64`。顶层 digest：Backend `sha256:31586cbc7595b25193560e9b3f50e6ec3c99b68503171a4198faceaa2bfa6030`；Web `sha256:013c5580bba2c909423bff93f5f2f85e0eabcbd5fdd03595311ebfd3bd2833d4`；Admin `sha256:170cc9f4025500cbe9299e47a45156e88c38b0f3bdf8e5ae54392e909c0895be`。
- 文档修订落实根规则与开发流程第 17 节：本机登录改为可选核验步骤，先查询再按错误处理鉴权；统一三份手册中已停用的远程缓存说明，不改凭据或降低发布门禁。缓存故障没有足够证据量化频率或唯一确定底层原因，不将推测写成平台缺陷。
- Docker 文档站读取遇到连接重置后，已读取 Docker 官方 GitHub 中的 Registry 缓存和 login 文档原文，确认缓存导出 `ignore-error=false` 默认值及本机凭据存储边界。修改范围采用 `project-hygiene` 的定向修订模式。
- 本次文档修订通过 `pnpm lint:md`、`pnpm check:documents`、`pnpm check:text`、`pnpm check:workspace`、`pnpm check:boundaries` 和技能文本卫生检查；Markdown 与文本卫生均检查 94 份文件，未发现问题。本次未重跑应用构建、镜像发布或生产部署。

## 剩余问题

- 原 CNB 查询 HTTP 403 和本机 TCR 鉴权阻塞已解除，历史失败记录保留用于追溯；三端正式镜像均已核验存在。
- 2026-09-14，用户明确要求将本计划更新为完成；以已记录的三端镜像核验和文档验证结果结束计划，本次不重新发布或执行 Git 交付。
- 1Panel 服务器拉取、运行和健康状态未验收，生产部署不在本计划授权范围内。
