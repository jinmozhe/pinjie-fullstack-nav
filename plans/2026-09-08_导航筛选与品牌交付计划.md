# 导航筛选与品牌交付计划

## 状态、范围与授权

- 状态：已结束
- 结果：已完成；本地筛选修复、品牌、契约与轻量检查完成，Git 交付结果以 PR 和最终回复为准。
- 授权：用户显式执行 `$git-sync`，覆盖当前全部本地可交付改动、必要修复、验证、提交、PR 和 rebase 合并。
- 关联需求：NAV-WEB-001、NAV-WEB-007、NAV-WEB-008、NAV-CAT-002；BASE-WEB-003、BASE-WEB-008、BASE-API-001、BASE-API-003。
- 目标：交付现有分类与标签交叉筛选、导航视觉微调、拾径品牌及静态 favicon，并同步遗漏的契约与文档。
- 排除：含数据库凭据的一次性本地抓取脚本、真实环境文件和数据不提交；不执行脚本、数据库操作、重型验证、发布或部署。

## 影响与规则落实

| 范围 | 是否涉及 | 工作与验证 |
| --- | --- | --- |
| Backend | 涉及 | 补齐筛选参数校验及管理员分类内标签查询，Ruff、格式、Mypy、导入边界、编译和应用导入 |
| Admin | 验证涉及 | 共享客户端生成后 typecheck、lint，页面逻辑不变 |
| Web | 涉及 | 交叉筛选错误与加载状态、受限数据隔离、窄屏布局、品牌和 favicon，typecheck、lint |
| API Client | 涉及 | 从后端导出根 OpenAPI，再生成客户端，检查重复生成无漂移 |
| Database | 不涉及 | 不改模型、迁移和数据 |
| Deployment | 不涉及 | 不改发布流程、不发布或部署 |
| Documentation | 涉及 | PRD、导航架构、视觉规范、品牌说明、索引和 Changelog；Markdown、文本、文档与工作区门禁 |

规则依据：根与 Backend/Web AGENTS.md、开发流程第 17 节和第 18.1 节、Backend 工程标准、模块边界、错误模型及测试策略。Router 保持协议入口，Service 校验筛选语义与可见性，Repository 复用可见站点条件；前端不把筛选请求失败显示为空选项，不保留失效身份的私有选项。

## 实施步骤与文件

1. 保存原分支、工作区原字节与暂存状态，建立隔离后端验证工作区，排除含凭据的本地脚本。
2. 修复 `apps/backend/app/{db/repositories,services,domains}/navigation*` 与 `apps/web/src/features/navigation/` 的交叉筛选缺口；保留已有视觉方向。
3. 保留现有 `uv.lock` 更新：click 8.5.0、coverage 7.16.0、grimp 3.16、import-linter 2.14、pydantic 2.13.5、pydantic-core 2.46.5、ruff 0.16.5、websockets 17.1；直接依赖声明不变，执行锁定安装与轻量兼容检查。
4. 导出 `openapi.json`，生成 `packages/api-client/src/`，补充与风险匹配的回归用例源码，按策略不运行 pytest/Vitest。
5. 核对品牌素材与 `apps/web/src/app/favicon.ico`，同步文档，执行轻量检查后提交与 PR 交付。

## 风险与恢复

- 原文件与分支保存在本次 Git 恢复记录中，逐文件核对后修改；恢复仅处理本次差异，不覆盖并发编辑。
- 交叉筛选遵守公开和管理员各自可见范围，错误返回明确状态；身份重新核验期间详情凭据继续暂停显示。
- 接口变化为新增可选查询与管理员只读标签入口，不改已存在响应类型；线上 Breaking Change 与 Security 检查由 PR 工作流执行。
- 无数据库迁移，应用回滚可恢复此前版本；本地含凭据脚本保持原路径和原字节，不纳入 Git 与隔离验证快照。

## 验证与剩余问题

- 已完成：隔离工作区 `uv sync --locked`、Backend Ruff/格式/Mypy/导入边界/编译/应用导入，Admin 与 Web typecheck/lint，治理命令各检查项；OpenAPI 二次导出和 API Client 二次生成逐字节一致。
- 修复：筛选查询的 Mypy 泛型变量冲突及 Ruff 格式、计划活动索引格式；复验通过。增加管理员标签 GET 的 Web BFF 精确白名单及 Cookie 隔离回归用例源码。
- 已补充：真实 PostgreSQL 分类标签可见性与错误语义断言、Web 筛选失败重试和退出清理回归用例源码。测试未运行，不据此宣称动态验收通过。
- 本地批处理脚本含凭据，原字节保留在原路径和受保护恢复副本；未执行、未暂存、未复制到隔离验证工作区。依赖更新与全部可交付代码纳入同一交付分支。
- 未执行：pytest、Vitest、production build、浏览器自动化、真实数据库与外站验证。
- 待确认问题：无。
