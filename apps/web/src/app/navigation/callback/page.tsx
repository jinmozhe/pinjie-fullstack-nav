import { ReaderCallback } from "@/features/navigation";

export const dynamic = "force-dynamic";
export const metadata = { title: "管理员登录", robots: { index: false, follow: false }, referrer: "no-referrer" as const };
export default ReaderCallback;
